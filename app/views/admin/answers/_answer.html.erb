<li class="mb-4">
  <%= answer.text %><br>

  <%= link_to t('helpers.links.show'), admin_answer_path(answer) %>
  |
  <%= link_to t('helpers.links.edit'), edit_admin_answer_path(answer) %>
  |
  <%= link_to t('helpers.links.delete'), admin_answer_path(answer),
              method: :delete, data: { confirm: t('helpers.links.confirm') } %>
</li>
