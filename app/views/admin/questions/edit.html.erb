<%= render template: 'tests/show' %>
<hr>

<h1><%= question_header(@question) %></h1>

<%= render 'form', model: [:admin, @question] %>

<%= link_to 'Back', admin_test_questions_path(@test) %>
|
<%= link_to 'Delete', admin_question_path(@question), method: :delete, data: { confirm: 'Are you sure?' } %>
