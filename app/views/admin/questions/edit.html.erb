<div class="test-header">
  <%= render template: 'tests/show' %>
</div>

<div class="m-3">
  <h3><%= question_header(@question) %></h3>

  <%= render 'form', model: [:admin, @question] %>

  <%= link_to t('helpers.links.back'), admin_test_questions_path(@test), class: 'mr-3' %>
  <%= link_to t('helpers.links.delete'), admin_question_path(@question), class: 'mr-3',
              method: :delete, data: { confirm: t('helpers.links.confirm') } %>
</div>
