<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>

  <% if user_signed_in? %>
    <span class="navbar-text m-auto"><%= t('layouts.application.welcome_header', name: current_user.name) %></span>

    <ul class="navbar-nav">
      <li class="nav-item">
        <%= link_to t('.tests'), root_path, class: 'nav-link' %>
      </li>

      <li class="nav-item">
        <%= link_to t('.awards'), awards_index_path, class: 'nav-link' %>
      </li>

      <% if current_user.admin? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= t('.control') %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to t('.tests'), admin_tests_path, class: 'dropdown-item' %>
            <%= link_to t('.badges'), admin_badges_path, class: 'dropdown-item' %>
            <%= link_to t('.gists'), admin_gists_path, class: 'dropdown-item' %>
          </div>
        </li>
      <% end %>

      <li class="nav-item">
        <%= link_to t('layouts.application.exit'), destroy_user_session_path, method: :delete, class: 'nav-link' %>
      </li>
    </ul>
  <% end %>
</nav>
