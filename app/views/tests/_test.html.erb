<tr>
  <td class="text-left"><%= test.title %></td>
  <td><%= test.questions.count %></td>
  <td><%= test_level(test) %></td>
  <td class="text-left"><%= button_to t('.start'), start_test_path(test), class: 'btn btn-primary' %></td>
</tr>
