<%= render template: 'tests/show' %>
<hr>
<%= render 'form', model: [@question], method: :put %>
<br>
<%= link_to 'Back', test_questions_path(@test) %>
|
<%= link_to 'Delete', question_path(@question), method: :delete, data: { confirm: 'Are you sure?' } %>
